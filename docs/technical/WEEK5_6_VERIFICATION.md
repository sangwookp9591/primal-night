# W5~6 검증 — 사체 해체 위험·보상 루프

작성일: 2026-07-18. 계획: `WEEK5_6_PLAN.md`. 커밋 범위: `8947529..769f42e` (계획 1 + 구현 4).

## 1. 판정 기준과 결과

계획의 판정 문장: **"사체를 해체해 자원을 얻고, 그 대가로 생긴 피 냄새와 소음을 감수하며
철수 시점을 고르는 플레이"** — 자동 하네스와 실기 플레이 양쪽에서 통과.

## 2. 구현 요약 (태스크 9/9)

| 커밋 | 레인 | 내용 |
|---|---|---|
| `fe48969` | B | sinew 아이템·뼈 긁개 재료 정본화(bone 2+sinew 1), 수분·포만 행동 효과(탈수 스태미나 페널티·포만 기반 자연 회복), 날고기 섭취(ConsumeAction, 호스트 검증) |
| `f0058ae` | C | 인벤토리·노트 화면(Tab, ui_focus_next 해제 — 계측 후 계획 §6 1안), PackCoordinator 랩터 2마리 조사 분담·측면 포위 |
| `b342299` | A | Carcass + E 홀드 해체(구간 25%, 돌칼 4×8초/뼈 긁개 4×6초 — 정본 §14.4 '20~40초 노출'), NetButcher yield_mask 호스트 권위, 절단 소음·단계별 피 냄새 |
| `769f42e` | A | SmellSource 실기 무취 핫픽스 + 해체 루프 하네스·성능 기준선 (W5-T9) |

완전 해체 산출(날고기4/뼈2/힘줄1)이 정확히 뼈 긁개 1개 재료 — 해체가 다음 해체를 25%
빠르게 만드는 고리가 닫힌다. 도구 시간 배수는 CarcassProfile 소유.

## 3. 자동 검증

```
GUT: Scripts 71 / Tests 419 / Passing 419 / 실패 0   (시작 시점 342)
해체 루프 하네스: seed 5001~5010 10/10 PASS, 철수 성공·실패 모두 관측
감지 루프 하네스: seed 4001~4010 10/10 PASS 유지
```

성능 기준선 `BASELINE_W5_6_BUTCHER.json` (랩터 2마리 + 사체 냄새 상태):
frame p95 6.94ms / scent p95 0.209ms / ai p95 0.106ms / 활성 셀 최대 33 — 전부 예산 안.
GUT 게이트 7개(`test_butcher_loop_contract.gd`)가 실씬 배선과 기준선을 잠근다.

## 4. 실기 플레이 검증 (GUI + 자동 입력)

- R 순환으로 돌 명시 선택 → C 돌칼 제작(발견 관찰문 표시) → 사체 접근 시
  대상 라벨 "랩터 사체 해체 (0/4)" → E 홀드 8.6초 → **날고기 2 획득과 동시에
  "추격!" 경고** — 위험·보상 루프의 긴장이 실기 그대로 발생.
- Shift 달리기 철수 → Tab 인벤토리·노트 화면: 16칸 슬롯·무게(4.8/20.0),
  발견 제작법과 관찰문 열람. 화면을 열어도 게임 진행(랩터 추격 유지).
- 랩터 무리(2마리)의 압박이 단독 대비 체감상 뚜렷하게 상승.

## 5. 실기 계측이 잡은 버그 (자동 테스트가 원리적으로 못 잡던 부류)

1. **SurvivalDemo 가 SmellGrid 보다 먼저 _ready** — 사체·바닥 raw_meat 의 SmellSource
   등록 강도 0.0. 특히 바닥 raw_meat 은 **W3-T4 이후 실기에서 한 번도 냄새를 낸 적이
   없었다.** call_deferred 등록 + `_released` 가드로 수정, 실씬 로드 회귀 테스트 6개.
2. **main.tscn 에 NetButcher 노드 부재** — 해체가 호스트 권위를 타지 않고 로컬로
   떨어짐(자체 하네스를 세우는 GUT 는 통과). 노드 추가 + 씬 계약 테스트 4개.

교훈: 자체 하네스 기반 GUT 는 "실씬에 배선됐는가"를 검증하지 못한다. 실씬 로드 계약
테스트(`test_*_scene_contract.gd`)를 신기능마다 두는 것을 컨벤션으로 확립.

## 6. 잔여 / 다음 마일스톤 이월

| 항목 | 상태 |
|---|---|
| 가죽·건조대·가죽 망토 | 소비처가 함께 필요 — 계획 §7 근거로 이월 |
| 두 번째 공룡 1종 | 해체 루프 검증 완료로 착수 조건 충족 — W7 후보 |
| 7일 세션 확장 | 4~7일차 콘텐츠(대이동·화산재·부품 B/C) 전제 — W7~8 이후 |
| 랩터 사망·전투 | 정본상 사체는 시드 배치 월드 오브젝트 — 전투 설계와 함께 |
| E 픽업 밀집 정밀도 | R 순환으로 실용 해소. 잔여 불편은 인벤토리 UI 고도화 때 재평가 |
