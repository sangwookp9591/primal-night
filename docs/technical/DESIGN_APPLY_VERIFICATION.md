# 디자인 에셋·맵 적용 검증

작성일: 2026-07-18. 커밋 범위: `ec8fdc5..86c2992` (7 커밋). 오케스트레이션 6레인.

## 1. 범위와 결과

07-16 디자인 산출물(디자인 시스템 보드·HUD 목업·스타일 가이드)과 미구현 초안
(PLAYER_CHARACTER_ASSET_DRAFT, MAP_DESIGN_DRAFT)을 실제 게임에 적용하고,
시나리오 기반 신규 에셋을 생성·연결했다. 회색 상자 단계 종료.

| 커밋 | 레인 | 내용 |
|---|---|---|
| `ec8fdc5` | H (5.5) | 잿불(Ember) 팔레트 Theme — HUD·인벤토리 적용, 기능 불변 |
| `adfa786` | P (5.5) | 플레이어 8방향 시트(기존 384×384) 씬 연결, velocity 방향 판정 |
| `a2e91a8` | M (Opus) | 40청크 계곡 맵 — 8×8 청크 40,960타일, Zone 5종, 랜드마크 16개, 게임플레이 좌표 불변 오프셋 |
| `dab04e2` | G (5.6 sol) | 에셋 5종 생성: 랩터 8방향 시트, 사체 4단계×8방향, 아이템 13종, 모닥불 4상태, 소음 미끼 |
| `f099d1b` | C2 (5.6 sol) | 아이템·모닥불·미끼 스프라이트 연결 (월드+인벤토리 아이콘 공유) |
| `6c6c543` | C1 (5.5) | 랩터 8방향 애니메이션·사체 해체 단계 시각 연결 |
| `86c2992` | 핫픽스 | 시각 노드 null 가드 — 스크립트 단독 경로 회귀 해소 |

## 2. 에셋 생성 기록

- 생성 담당: **gpt-5.6-sol** (정책 §1 고정). 중간에 모델 용량 초과로 지연 → 재시도로 복구.
- 방향 세트는 정책 §2대로 **8방향 전체를 단 한 번의 요청으로** 생성 — 방향 간 개체
  정합성 유지 확인 (코디네이터 육안 검수).
- 원본 `assets/source/` / 런타임 `assets/sprites/` 분리, 공통 42색 팔레트 양자화.
  규격 문서: `docs/design/PRIMAL_NIGHT_ASSET_SHEETS_DRAFT.md`.

## 3. 검증

```
GUT: Scripts 78 / Tests 455 / Passing 455 / 실패 0   (시작 시점 419)
감지 루프 하네스 10/10, 해체 루프 하네스 10/10 유지
headless frame p95 6.944ms (기준선 동일 — 로직·물리 비용)
```

실기 플레이: Zone 색 지형(Z01 분지/Z03 밀림 경계), 플레이어 스프라이트 8방향
전환(S→SW 확인), 아이템 13종 개별 스프라이트, 모닥불 돌 고리, 랜드마크 마커,
잿불 HUD. GPU 렌더 포함 프레임은 실기에서 16.67ms 유지 (vsync 상한).

## 4. 잡은 회귀

C2 의 `world_item.gd` 스프라이트 할당이 씬 없이 스크립트만 생성하는 기존 테스트
경로에서 null 크래시 → 냄새 등록 중단. 레인 자체 검증(tests/items)은 통과했으나
**통합 전체 GUT 게이트**가 tests/senses 에서 검출 — get_node_or_null 가드로 수정.
교훈: 시각 연결은 "시각 없는 인스턴스에서도 게임플레이 로직이 도는가"를 기본
가드로 삼는다 (Campfire/CampfireSite 에 선제 적용).

## 5. 잔여 / 다음 이월

| 항목 | 상태 |
|---|---|
| 정식 지형 타일 아트 | 현재 Zone 색 임시 타일(프로그램 생성). 스타일 가이드 기반 타일 시트 생성은 다음 에셋 배치 |
| 랜드마크 시각물 (헬기 꼬리·안테나 등) | 마커만 배치됨. 랜드마크별 프롭 에셋은 다음 배치 |
| 걷기 외 애니메이션 (공격·채집·부상) | 초안 명세대로 이번 범위 제외 |
| GPU 프레임 정밀 측정 | vsync 상한에서 여유 확인. 저사양 기준은 출시 판정 시 |
